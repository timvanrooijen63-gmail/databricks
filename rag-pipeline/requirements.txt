# Docs agent dependencies.
# Pinned floor = a version known to expose the APIs used here; capped at the next
# major to keep CI and the Databricks %pip install (notebooks/register_agent.py)
# in the same band. The notebook installs with --upgrade, so the served runtime
# may be newer — keep these ranges compatible with that.

# ChatDatabricks + DatabricksMCPServer + DatabricksMultiServerMCPClient.
databricks-langchain>=0.20.0,<1.0.0

# DatabricksMCPClient.get_databricks_resources() (for logging) + MCP transport.
databricks-mcp>=0.4.0,<1.0.0

# create_react_agent (the ReAct agent loop).
langgraph>=0.3.0,<1.0.0

# MLflow 3.x: ResponsesAgent interface + model-from-code logging.
mlflow>=3.13.0,<4.0.0

# Agent Framework: agents.deploy() to Model Serving.
databricks-agents>=0.16.0,<1.0.0

# WorkspaceClient (usually transitive via the above; pinned for clarity).
databricks-sdk>=0.30.0,<1.0.0

# Testing (not installed on Databricks clusters).
pytest>=8.0,<10.0
